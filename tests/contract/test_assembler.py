import pytest


def test_assembler_round_trip():
    # B's emitter -> do not touch emit/ — write the round-trip property test (200 generated programs) and hand it over
    pytest.skip("Track B: Implement the round-trip property test here once Program/serialize is available. Generate 200 random Programs and assert deserialize(serialize(p)) == p.")
