import pytest


def test_emulator_end_to_end():
    # D's integration -> debug the end-to-end per-tier failures with the fixture in one hand and the program text in the other
    pytest.skip("Track D: End-to-end integration tests using the emulator against the fixtures go here.")
