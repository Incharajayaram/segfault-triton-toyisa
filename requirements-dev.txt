# Dev-only. Deliberately excludes triton and torch: the whole test suite must
# run on a machine with no GPU and no Triton, because the fixtures are checked
# in and all expectations come from fixtures/GOLDEN.json.
-numpy>=1.26
-pyyaml>=6.0
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
